{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ci-build-tests",
            "command": "scons",
            "type": "shell",
            "args": ["--build=debug", "-j", "12", "--tests-only"]
        },
        {
            "label": "ci-build",
            "command": "scons",
            "type": "shell",
            "args": ["--build=debug", "-j", "12", "--no-tests"]
        }
    ]
}